# ccLoad v4.6.10-beta.2 定制版升级实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 在官方 `v4.6.10-beta.2` 上恢复线上定制能力，验证生产 PostgreSQL 副本迁移，并构建可验收的本地二进制和容器镜像。

**架构：** 升级分支从官方标签 commit `3fbcf9e2` 创建。旧分支只作为行为和测试参考，所有定制功能按 beta 当前的存储、代理、OAuth、WebSocket 和设置接口重新接入；协议转换目录保持 beta 快照。每项能力独立测试和提交，数据库验证只连接生产库副本。

**技术栈：** Go 1.26、Gin、PostgreSQL/pgx、Redis、Node.js、Docker、SSE/WebSocket、OpenAPI 3.1

---

## 文件结构

- 创建 `internal/model/usage_event.go`、`internal/eventbus/*`、`internal/app/event_publisher.go`、`internal/app/usage_event_environment.go`：Redis 用量事件。
- 修改 `internal/model/log.go`、`internal/storage/sql/query.go`、`internal/app/handlers.go`、`internal/app/stats_cache.go`：多 Auth Token 日志过滤。
- 创建 `internal/app/static.go`、`internal/app/openapi_test.go`、`web/docs.html`、`web/openapi.yaml`：Swagger UI 和管理 OpenAPI。
- 修改 `internal/storage/sql/config.go`、`internal/storage/migrate_postgres_test.go`：PostgreSQL `disabled` int2 编码。
- 创建 `docs/redis-usage-event.md`：事件契约。

### 任务 1：固定 beta 基线

- [x] **步骤 1：创建隔离 worktree**

运行：`rtk git worktree add .worktrees/upgrade-v4.6.10-beta.2-custom -b kkw/upgrade-v4.6.10-beta.2-custom v4.6.10-beta.2`

结果：分支基于 `3fbcf9e284e71d3c038ff0680f37551d0d3708e5`。

- [x] **步骤 2：运行基线 Go 测试**

运行：`rtk go test -tags sonic ./internal/...`

结果：`3826` 项测试通过，退出码 0。

- [x] **步骤 3：运行基线 Web 和协议测试**

运行：`rtk make verify-web`

运行：`rtk bash .agents/skills/sync-cliproxy-core/scripts/verify.sh --tests`

结果：两条命令退出码均为 0；协议快照 commit 为 `2b9a9d23d2226efdea25d9710f217208cb52ff8b`。

### 任务 2：撤销本轮 authz 定制

**提交：**
- 撤销：`93fcac38 feat(authz): restore environment management`
- 撤销：`1a4e50ae feat(authz): enforce external auth on proxy routes`
- 撤销：`dd98a7ee feat(authz): restore external authorization service`
- 撤销：`5ccdc3dd feat(authz): restore environment persistence`

- [x] **步骤 1：保留当前任务 6 的未提交修改**

运行：`rtk git status --short`

预期：仅 Redis usage、499 计费相关文件和依赖处于未提交状态。

- [x] **步骤 2：按逆序创建 revert 提交**

运行：`rtk git revert --no-edit 93fcac38 1a4e50ae dd98a7ee 5ccdc3dd`

预期：生成 4 个可审计的 revert 提交，不修改 Redis usage 未提交文件。

- [x] **步骤 3：验证 authz 业务已完全移除**

运行：`rtk rg -n -i 'external_auth|external-auth|X-Sedna-Env|authz' internal web --glob '!internal/protocol/cliproxy/**'`

预期：没有本轮定制 authz 的模型、设置、路由、服务、测试或前端残留。

- [x] **步骤 4：运行 beta 本地鉴权回归**

运行：`rtk go test -tags sonic ./internal/app ./internal/storage/... -run 'RequireAPIAuth|AuthToken|ResponsesWebsocket' -count=1`

预期：官方 beta 本地 Token 和 WebSocket 鉴权测试全部通过。

### 任务 6：恢复 Redis 用量事件和 499 中性计费

**文件：**
- 创建：`internal/model/usage_event.go`
- 创建：`internal/model/token_stat.go`
- 创建：`internal/eventbus/publisher.go`
- 创建：`internal/eventbus/worker.go`
- 创建：`internal/eventbus/redis.go`
- 创建：`internal/eventbus/publisher_test.go`
- 创建：`internal/eventbus/worker_test.go`
- 创建：`internal/eventbus/redis_test.go`
- 创建：`internal/app/event_publisher.go`
- 创建：`internal/app/usage_event_environment.go`
- 创建：`internal/app/usage_event_test.go`
- 修改：`internal/app/log_service.go`
- 修改：`internal/app/proxy_handler.go`
- 修改：`internal/app/proxy_error.go`
- 修改：`internal/app/proxy_util.go`
- 修改：`internal/app/server.go`
- 修改：`internal/storage/store.go`
- 修改：`internal/storage/hybrid_store.go`
- 修改：`internal/storage/sql/auth_tokens.go`
- 修改：`internal/storage/sql/auth_tokens_update_stats_test.go`
- 修改：`go.mod`
- 修改：`go.sum`
- 创建：`docs/redis-usage-event.md`

- [x] **步骤 1：写 Publisher、落库后发布和 499 失败测试**

```go
func TestLogServicePublishesAttemptsOnlyAfterBatchAddLogs(t *testing.T) {
    // BatchAddLogs 失败时不发布；成功时每条日志发布一个 attempt 事件。
}

func TestClientCanceledWithUsageIsBilledNeutral(t *testing.T) {
    // token/cost 增加，SuccessCount 和 FailureCount 均不增加，status 保持 499。
}
```

同时覆盖 stream/pubsub 路由、environment 名规范化、队列满 fail-open、关闭时 drain、Redis 错误不影响代理。

- [x] **步骤 2：运行测试确认失败**

运行：`rtk go test -tags sonic ./internal/eventbus ./internal/app ./internal/storage/sql -run 'UsageEvent|Publisher|BilledNeutral|499' -count=1`

- [x] **步骤 3：实现事件和中性计费**

`TokenStatOutcome` 明确拆分：

```go
const (
    TokenStatSuccess TokenStatOutcome = iota + 1
    TokenStatFailure
    TokenStatBilledNeutral
)
```

SQL 更新以 `billFlag` 控制 token/cost，以 outcome 分别控制成功和失败计数。request 事件在请求结束发布；attempt 事件只在 `BatchAddLogs` 成功后发布。Redis DSN 为空时注入 NoopPublisher。

- [x] **步骤 4：运行定向测试并提交**

运行：`rtk go test -tags sonic ./internal/eventbus ./internal/app ./internal/storage/... -run 'UsageEvent|Publisher|BilledNeutral|499' -count=1`

运行：`rtk git add internal/model internal/eventbus internal/app internal/storage go.mod go.sum docs/redis-usage-event.md`

运行：`rtk git commit -m 'feat(eventbus): restore usage publishing and neutral billing'`

### 任务 7：恢复多 Token 日志过滤

**文件：**
- 修改：`internal/model/log.go`
- 修改：`internal/storage/sql/query.go`
- 修改：`internal/storage/sql/query_test.go`
- 修改：`internal/app/handlers.go`
- 修改：`internal/app/handlers_test.go`
- 修改：`internal/app/dashboard_scope.go`
- 修改：`internal/app/stats_cache.go`

- [ ] **步骤 1：写多值过滤失败测试**

```go
filter := &model.LogFilter{AuthTokenIDs: []int64{11, 22}}
query, args := NewQueryBuilder(dialect).ApplyFilter(filter).Build()
if !strings.Contains(query, "auth_token_id IN") || len(args) != 2 {
    t.Fatalf("query=%q args=%v", query, args)
}
```

HTTP 参数同时接受重复参数和逗号分隔值，去重且拒绝非正整数；单值继续映射为一个元素。

- [ ] **步骤 2：运行测试确认失败**

运行：`rtk go test -tags sonic ./internal/app ./internal/storage/sql -run 'MultipleAuthTokens|AuthTokenIDs' -count=1`

- [ ] **步骤 3：实现并验证**

`LogFilter` 将 `AuthTokenID *int64` 改为 `AuthTokenIDs []int64`，WhereBuilder 对一个值使用等号、多个值使用 `IN`。缓存键必须包含排序去重后的完整 Token ID 列表。

运行：`rtk go test -tags sonic ./internal/app ./internal/storage/sql -run 'MultipleAuthTokens|AuthTokenIDs|LogFilter' -count=1`

- [ ] **步骤 4：提交**

运行：`rtk git add internal/model/log.go internal/storage/sql internal/app`

运行：`rtk git commit -m 'feat(logs): restore multi-token filtering'`

### 任务 8：恢复 Swagger UI 和更新 OpenAPI

**文件：**
- 创建：`internal/app/static.go`
- 创建：`internal/app/openapi_test.go`
- 修改：`internal/app/server.go`
- 创建：`web/docs.html`
- 创建：`web/openapi.yaml`
- 修改：`README.md`
- 修改：`README.zh-CN.md`

- [ ] **步骤 1：写静态路由失败测试**

覆盖 `/docs/`、`/docs/openapi.yaml` Content-Type、禁止目录遍历、未知文件 404，以及 OpenAPI 3.1 可解析、所有声明路径在 beta 路由中存在。

- [ ] **步骤 2：运行测试确认失败**

运行：`rtk go test -tags sonic ./internal/app -run 'OpenAPI|DocsStatic' -count=1`

- [ ] **步骤 3：恢复 UI 并基于 beta 路由更新规范**

OpenAPI 必须包含 OAuth credential import、Codex/Antigravity/xAI 管理接口、运行指标和多 Token 日志参数。管理员 Bearer auth 定义为全局 security scheme。

- [ ] **步骤 4：验证并提交**

运行：`rtk go test -tags sonic ./internal/app -run 'OpenAPI|DocsStatic|Admin' -count=1`

运行：`rtk git add internal/app web README.md README.zh-CN.md`

运行：`rtk git commit -m 'feat(docs): restore management OpenAPI and Swagger UI'`

### 任务 9：验证 PostgreSQL int2 和超时设置兼容

**文件：**
- 修改：`internal/storage/sql/config.go`
- 修改：`internal/storage/migrate_postgres_test.go`
- 修改：`internal/storage/migrate_sqlite_test.go`
- 修改：`internal/app/server_misc_test.go`

- [ ] **步骤 1：写 PostgreSQL 回归测试**

测试使用 `ModelEntry{Disabled: true}` 创建和更新渠道，断言写入参数为 `int16(1)`/`int16(0)`，并从 PostgreSQL 读回 disabled 状态。迁移测试预置 `non_stream_timeout=300` 及协议覆盖，迁移后值保持不变。

- [ ] **步骤 2：运行测试确认旧编码问题**

运行：`rtk go test -tags sonic ./internal/storage -run 'Postgres.*Disabled|Preserve.*NonStreamTimeout' -count=1`

预期：如果 beta 已修复则记录等价实现并只补兼容测试；如果仍传 bool，则 PostgreSQL 测试复现 int2 编码失败。

- [ ] **步骤 3：实施最小修复**

`saveModelEntriesTx` 统一传入：

```go
disabled := int16(0)
if entry.Disabled {
    disabled = 1
}
```

迁移使用 insert-if-missing，不更新已有超时设置。

- [ ] **步骤 4：运行测试并提交**

运行：`rtk go test -tags sonic ./internal/storage ./internal/app -run 'Disabled|NonStreamTimeout' -count=1`

运行：`rtk git add internal/storage internal/app/server_misc_test.go`

运行：`rtk git commit -m 'fix(postgres): preserve model and timeout compatibility'`

### 任务 10：全量回归和协议审计

- [ ] **步骤 1：运行定制功能组合测试**

运行：`rtk go test -tags sonic ./internal/app ./internal/storage/... ./internal/eventbus/... -run 'ExternalAuth|UsageEvent|BilledNeutral|AuthTokenIDs|OpenAPI|Disabled|NonStreamTimeout' -count=1`

- [ ] **步骤 2：运行协议快照审计**

运行：`rtk bash .agents/skills/sync-cliproxy-core/scripts/verify.sh --tests`

预期：仍为 beta 的固定 commit；旧分支协议目录没有覆盖新快照。

- [ ] **步骤 3：运行全量测试和 lint**

运行：`rtk go test -tags sonic ./internal/...`

运行：`rtk make verify-web`

运行：`rtk golangci-lint run ./...`

运行：`rtk git diff --check`

- [ ] **步骤 4：运行并发测试**

运行：`rtk make race-fast`

如果 race-fast 发现共享状态问题，修复后运行：`rtk make race`。

### 任务 11：构建本地制品

- [ ] **步骤 1：构建本机二进制**

运行：`rtk make build VERSION=v4.6.10-beta.2-custom`

预期：生成可运行的本机 `ccload`，`/public/version` 返回 custom 版本。

- [ ] **步骤 2：构建 Linux amd64 二进制**

运行：`rtk env CGO_ENABLED=0 GOOS=linux GOARCH=amd64 make build BINARY_NAME=ccload-linux-amd64-v4.6.10-beta.2-custom VERSION=v4.6.10-beta.2-custom`

运行：`rtk file ccload-linux-amd64-v4.6.10-beta.2-custom`

运行：`rtk shasum -a 256 ccload-linux-amd64-v4.6.10-beta.2-custom`

- [ ] **步骤 3：构建本地容器镜像**

运行：`rtk docker build --build-arg VERSION=v4.6.10-beta.2-custom --build-arg COMMIT=$(git rev-parse --short HEAD) -t ccload:v4.6.10-beta.2-custom .`

运行：`rtk docker image inspect ccload:v4.6.10-beta.2-custom`

### 任务 12：生产 PostgreSQL 副本迁移测试

- [ ] **步骤 1：确认目标是副本而非生产库**

通过 `YiSuo中转站` 只读取 ccLoad 服务配置，解析生产 PostgreSQL DSN；使用 `pg_dump --format=custom --no-owner --no-acl` 生成一致性备份。任何后续 ccLoad 进程的 DSN 必须指向新建的本地临时 PostgreSQL 容器，禁止指向生产主机地址。

- [ ] **步骤 2：记录备份校验信息**

记录备份绝对路径、字节数和 `rtk shasum -a 256` 输出。备份不得加入 Git，任务结束不自动删除。

- [ ] **步骤 3：恢复到临时 PostgreSQL**

启动独立 PostgreSQL 容器，创建 `ccload_beta_migration_test` 数据库，使用 `pg_restore --clean --if-exists --no-owner --no-acl` 恢复备份。恢复完成后记录 `system_settings`、`auth_tokens`、`channels` 和 `logs` 行数及关键设置值。

- [ ] **步骤 4：使用 custom beta 执行首次迁移和幂等启动**

使用本地镜像连接临时 PostgreSQL，第一次启动等待 `/health` 成功后停止；第二次使用相同数据库启动并再次等待 `/health`，确认无重复列、重复索引或默认值覆盖错误。

- [ ] **步骤 5：运行迁移后验证**

验证：

- 所有全局和协议级非流式超时值未改变；
- OAuth 新列/表存在；
- disabled 模型可保存并读回；
- 多 Token 日志查询返回两个 Token 的并集；
- `/docs/openapi.yaml`、`/public/version` 和管理员环境 CRUD 冒烟通过。

- [ ] **步骤 6：保存验收记录**

将不含凭据和用户数据的命令、行数摘要、镜像 ID、二进制 SHA-256、备份 SHA-256 和测试结果写入 `docs/superpowers/verification/2026-08-09-v4.6.10-beta.2-custom.md`，强制加入 Git 并提交：

运行：`rtk git add -f docs/superpowers/verification/2026-08-09-v4.6.10-beta.2-custom.md`

运行：`rtk git commit -m 'docs: record v4.6.10 beta custom verification'`

### 任务 13：最终需求核对

- [ ] **步骤 1：核对规格中的每项保留能力**

逐项对应代码、测试和验证输出：Redis usage、Token environment stream 路由、499 中性计费、多 Token 日志、Swagger、int2 修复、300 秒超时保持。

- [ ] **步骤 2：确认生产未变更**

只读检查线上 ccLoad 版本和服务启动时间，确认仍为升级前版本；不执行上传、重启、数据库迁移或 CPA 操作。

- [ ] **步骤 3：报告实际结果**

报告分支、commit、通过/失败的每条验证、数据库副本路径和校验值、本地镜像 ID、二进制路径及 SHA-256。任一验收项失败时明确标记未完成，不进入部署。
