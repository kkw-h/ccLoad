# ccLoad v4.6.10-beta.2-custom 验收记录

验收日期：2026-08-09

## 范围

- 基线：官方 `v4.6.10-beta.2`，commit `3fbcf9e284e71d3c038ff0680f37551d0d3708e5`
- 分支：`kkw/upgrade-v4.6.10-beta.2-custom`
- 验收代码 commit：`bf491299`
- 本轮不包含外部 authz、`X-Sedna-Env`、外部鉴权环境管理或代理链路改造。
- 未部署、未重启线上 ccLoad/CPA、未写入生产 PostgreSQL。

## 代码与测试

- CLIProxy 协议快照：`2b9a9d23d2226efdea25d9710f217208cb52ff8b`，审计脚本 44 项通过。
- `go test -tags sonic ./internal/...`：通过，3,861 项测试。
- `make verify-web`：通过。
- `golangci-lint run ./...`：通过，无问题。
- `make race-fast`：通过；`internal/app` race 测试耗时 154.531 秒。
- `git diff --check`：通过。
- authz 源码与 OpenAPI 扫描：未发现 `authz`、`X-Sedna-Env` 或外部鉴权环境接口。
- Redis usage 事件安全回归：事件不再序列化可直接重放的 `token_hash`，继续使用 `auth_token_id` 与 `environment` 关联。
- 非法 Token 日志过滤安全回归：参数存在但全部非法时使用不匹配哨兵，避免退化为全量查询。

## 构建制品

| 制品 | 路径或 ID | SHA-256 |
| --- | --- | --- |
| macOS arm64 | `ccload-darwin-arm64-v4.6.10-beta.2-custom` | `5032458d13403e21e43bb376ecfe74e0722585544cc265ca32396f44e63c79ec` |
| Linux amd64 静态二进制 | `ccload-linux-amd64-v4.6.10-beta.2-custom` | `ebf91d2c95ccee32f7998564d0f1f12de6d062cf7dccafe7dc2e7e16640dcd8c` |
| Docker arm64 镜像 | `sha256:9104203f5e9ed8acbacfcb221ec48550888b43072e70095d8cdd5710e32a8470` | 镜像内 commit `bf49129` |

Docker 标签：`ccload:v4.6.10-beta.2-custom`、`ccload:latest`。

## 生产 PostgreSQL 只读备份

- 来源：`YiSuo中转站` 本机 PostgreSQL 16 的 `ccload` 数据库。
- 方式：通过 SSH 执行只读 `pg_dump --format=custom --no-owner --no-acl`，原样流式保存到本地。
- 备份路径：`/tmp/ccload-v4.6.10-beta.2-migration.6jyj6Y/ccload-production-20260809.dump`
- 字节数：`3,159,171,233`
- SHA-256：`14c5972911dda6a78b47fbdd508c7b12ede42f9a2e137b6344614aac6ea1dabf`
- `pg_restore --list`：通过。
- 备份保留，未加入 Git，未自动删除。

## 副本迁移

临时 PostgreSQL 容器：`ccload-beta-migration-pg-20260809`，数据库 `ccload_beta_migration_test`。恢复后仅通过 `127.0.0.1:55432`/`host.docker.internal:55432` 连接，没有向生产 DSN 启动 custom beta。

迁移前摘要：

| 对象 | 数量或值 |
| --- | ---: |
| `system_settings` | 53 |
| `auth_tokens` | 26 |
| `channels` | 5 |
| `logs` | 18,241 |
| `debug_logs` | 29,765 |
| `non_stream_timeout` | 300 |
| 协议级 `*_non_stream_timeout` | 均为 0 |
| `channel_models.disabled` | 14 条为 0 |

第一次启动执行 beta schema 迁移并返回 `/health` 200。第二次及最终镜像启动均返回 `/health` 200，日志中未再次执行 schema 迁移，没有重复列、重复索引或默认值覆盖错误。

迁移后摘要：

| 对象 | 数量或值 |
| --- | ---: |
| `system_settings` | 58 |
| `auth_tokens` | 26 |
| `channels` | 5 |
| `logs` | 18,241 |
| `non_stream_timeout` | 300 |
| 协议级 `*_non_stream_timeout` | 均为 0 |

OAuth 兼容列已存在：`channels.auth_type`、`channels.oauth_credential`、`channels.channel_type`、`channels.websockets`、`channels.protocol_transform_mode`。

## 功能冒烟

- 管理员登录成功并取得 Web Session Bearer Token。
- 多 Token 日志过滤：Token 14 单独为 5,621 条；Token 14 和 19 使用重复参数或逗号参数均为 8,669 条。
- PostgreSQL disabled 写入：通过管理 API 创建禁用模型，数据库读回 `smallint=1`，管理 API 读回 `disabled=true`；测试渠道已删除，最终渠道数恢复为 5。
- `/docs/`：200。
- `/docs/openapi.yaml`：200，包含多 Token、Codex OAuth、Antigravity、xAI、runtime metrics；不包含 authz。
- `/public/version`：200，返回 `v4.6.10-beta.2-custom`。
- OpenAPI 已补齐管理员登录必填 `mode` 和渠道必填 `auth_type`，最终镜像实际返回修正后的 schema。

## 已确认兼容性影响

生产副本原有 `debug_log_enabled=true`，但 `debug_log_retention_minutes=52560000`，超过 beta 当前允许的 `1–1440`。beta 将该值视为无效并回退到默认短保留期，首次启动后 29,765 条历史 `debug_logs` 被清理为 0。

该行为不影响普通 `logs`、Token、渠道、模型和 300 秒非流式超时，但属于部署前必须明确接受或另行处理的破坏性变化。本轮不修改官方 beta 的调试日志保留策略。

因此当前分支可以用于继续评审和制定上线方案，但生产部署状态为阻塞：必须先决定保留、迁移或接受清理这些历史调试日志。

## 生产未变更确认

- 线上服务仍为 `ccLoad v3.13.3-custom-c06ce40 with PostgreSQL`。
- Main PID：`857198`。
- 启动时间：`2026-08-04 07:51:20 UTC`。
- 线上行数只读复查：`auth_tokens=26`、`channels=5`、`logs=18,241`。
- 未上传二进制、未重启服务、未执行生产迁移、未修改 CPA。

## 结论

代码整合、本地构建和生产 PostgreSQL 副本迁移测试已完成。计划内核心数据、接口和超时设置均通过验收，代码审查发现的 Redis 凭据泄漏与非法 Token 过滤退化问题已经修复并加入回归测试。生产部署仍因 legacy 调试日志清理影响而阻塞；本记录不代表已批准或已执行生产部署。
