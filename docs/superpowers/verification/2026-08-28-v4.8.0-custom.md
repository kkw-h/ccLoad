# v4.8.0-custom 升级验收记录

- 验收日期：2026-08-28（Asia/Shanghai）
- 上游基线：`v4.8.0` (`7622d520f7f453e3d7121bd39e690c4e0e4259fa`)
- 定制合并提交：`45a16de4effcce3afae1bcd0dd93d6bff605c7a7`
- 分支：`kkw/upgrade-v4.8.0-custom`
- 候选版本：`v4.8.0-custom-45a16de4`
- Linux amd64 SHA-256：`04fdd3cee2263908b6920ffe5d932eef6ac7b4696740ca0df286a36c87ebfea9`
- 公开 Tag、GitHub Release 和容器镜像：未创建

## 同步范围

- 合并官方 `v4.8.0`，保留 ccLoad 定制的 Redis usage event、token environment、attempt sequence、模型推理和元数据覆盖、管理端模型字段及渠道管理能力。
- 同步 CLIProxyAPI 到 `v8.75.0`，并把核心审计链从定制分支原基线 `v8.69.0` 补齐到 `v8.75.0`。
- 兼容上游 multimodal fallback 热重载和 token status 类型变化。
- 计费路径继续只汇总一次工具成本，避免 `computeRequestCost` 已含工具成本后发生重复计费。

## 代码验证

- CLIProxy 核心审计：通过；43 个 core 文件、12 个 provider 文件、7 个明确排除文件、55 条 review 记录、68 个转换快照测试和 1 个 provider 快照。
- CLIProxy converter/provider 测试：通过。
- 热点回归测试：通过，共 4,185 个测试。
- `go test -tags sonic ./internal/...`：通过，共 5,358 个测试、49 个包。
- `internal/app` 与 `internal/storage/sql` 最终复验：通过，共 2,797 个测试。
- `make verify-web`：通过。
- 独立 Linux amd64 构建：通过。
- `golangci-lint run ./...`：通过，无问题。
- `make race-fast`：通过；`internal/app` race 插桩运行 224.693 秒，SQL 存储和协议高价值竞态测试同时通过。
- `git diff --check`：通过。

race 检查最初暴露了管理 check-in 初始扫描与测试替换 store 的测试夹具竞态。测试服务器改为在构造时注入 store，并等待初始扫描完成后再继续；三个相关 race 测试各重复 10 次均通过，随后完整 `make race-fast` 通过。运行时代码行为未因此扩大。

## 服务器备份

- PostgreSQL 备份：`/var/backups/ccload/ccload-v4.7.0-before-v4.8.0-20260828T060812Z.dump`
- 格式：PostgreSQL custom format
- 大小：510,739,138 字节
- SHA-256：`46dc29f308194272c5fd7d292c93d4dfd4f99c89b5c857d3237a5425cff88d50`
- 旧二进制回滚文件：`/usr/local/bin/ccload.v4.7.0-custom-7997453a.20260828T060812Z.bak`
- 旧二进制 SHA-256：`540a133c05ca173a6465192d0ece77db90d98d49af5917d1ac778ae6470a3172`
- 旧 systemd unit：`/etc/systemd/system/ccload.service.20260828T060812Z.bak`

备份仅保存在服务器，未下载到本地。

## PostgreSQL 副本迁移验证

- 测试库：`ccload_upgrade_v480_20260828t060812z`
- 候选端口：`127.0.0.1:18081`
- Redis：候选实例显式禁用，未向生产 usage stream 发布事件。
- 使用同一数据库副本连续启动候选版本两次，两个实例都返回 `v4.8.0-custom-45a16de4`，健康检查均通过。
- 迁移前关键数据：渠道 5、API Token 29、系统设置 64。
- 迁移后关键数据：渠道 5、API Token 29、系统设置 65；第二次启动计数保持不变。
- `model_metadata_overrides`、`model_multimodal_fallback` 和 `model_reasoning_effort_overrides` 均存在。
- 两次候选启动日志均未发现重复迁移、重复加列、fatal 或 panic。

验收完成后，测试数据库、临时上传二进制、临时部署脚本和瞬态候选单元均已清理。

## 生产状态

- 部署主机：`YiSuo中转站`
- 服务：`ccload.service`，`active/running`
- systemd Description：`ccLoad v4.8.0 custom`
- 线上版本：`v4.8.0-custom-45a16de4`
- 线上二进制：`/usr/local/bin/ccload`
- 线上二进制 SHA-256：`04fdd3cee2263908b6920ffe5d932eef6ac7b4696740ca0df286a36c87ebfea9`
- `/health`：`ok`
- `/public/version`：`v4.8.0-custom-45a16de4`
- PostgreSQL：渠道 5、API Token 29、系统设置 65
- `non_stream_timeout`：300 秒
- 模型元数据、多模态 fallback 和推理强度覆盖设置：保留
- Redis usage event publisher：启动日志确认启用
- 启动日志未发现 fatal、panic 或迁移失败

部署采用预先落盘、停服后原子替换、健康与精确版本门禁的方式；门禁失败脚本会自动恢复旧二进制。本次门禁一次通过，未触发回滚。
