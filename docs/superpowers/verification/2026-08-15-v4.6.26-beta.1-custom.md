# v4.6.26-beta.1-custom 升级验收记录

- 验收日期：2026-08-15（Asia/Shanghai）
- 上游基线：`v4.6.26-beta.1` (`535858b545b0dadeb9b4acca28bfb783918c8b18`)
- 候选提交：`b67b121819ddef0c269e8eebf72b76cf7b15087d`
- 候选版本：`v4.6.26-beta.1-custom-b67b121`
- Linux amd64 SHA-256：`06de27f9e833b51464b842bf7571105eefd6f0ef91419efb538db6251c45a684`
- 外部 authz、`X-Sedna-Env` 和外部鉴权环境管理：未包含

## 代码验证

- `go test -tags sonic ./internal/...`：通过，43 个包、4294 个测试。
- `make verify-web`：通过。
- `golangci-lint run ./...`：通过，无问题。
- `make race-fast`：通过；`internal/app` 在 race 插桩下运行 307.275 秒。
- `git diff --check v4.6.26-beta.1..HEAD`：通过。
- PostgreSQL `int2` 模型禁用参数与 300 秒非流式超时保留回归测试：通过。

`race-fast` 首次运行时，上游的 101 条 xAI OAuth 导入测试因 race 插桩超过固定 10 秒而超时；测试等待上限调整为 30 秒后，定向 race 测试和完整 `race-fast` 均通过。该调整不改变运行时代码。

## 服务器备份

- 备份文件：`/var/backups/ccload/ccload-pre-v4.6.26-beta.1-20260815T061030Z.dump`
- 格式：PostgreSQL custom format，`--no-owner --no-acl`
- 大小：101,198,236 字节
- SHA-256：`8f4b586e14edcdcc4cbb9d8fc521ced407d0d677b2310fd3c21f42202467e72a`
- 备份仅保存在服务器，未下载到本地。

## PostgreSQL 副本迁移验证

- 测试库：`ccload_upgrade_v4626_20260815t061030z`
- 候选实例端口：`127.0.0.1:18081`
- Redis：测试实例禁用，未向线上 usage stream 发布事件。
- 使用同一数据库副本连续启动候选版本两次，健康检查和版本接口均通过。
- 数据前后保持：渠道 5、API Token 27、调试日志 1678。
- 系统设置共 63 项；`non_stream_timeout=300`，四个协议级非流式超时均保持 `0`。
- 新版 `channels.available_time_start`、`channels.available_time_end` 列存在。
- `/v1/models`、Anthropic 模型列表、`/admin/channels` 的推理与模型元数据字段可解析。
- `/docs/openapi.yaml` 包含 `supported_reasoning_efforts` 和 `model_metadata_overrides`。
- 启动日志未发现 fatal、panic、迁移失败或数据竞争。

## 当前状态

代码、构建与数据库副本迁移验收通过。生产二进制替换和服务重启尚未在本记录创建时执行。
